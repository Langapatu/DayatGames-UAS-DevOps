@extends('layouts.app')
@section('title', 'Orders — Admin DayatGames')
@section('content')
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm uppercase tracking-widest text-cyan-300">Admin</p><h1 class="text-3xl font-black text-white">Orders</h1></div><form method="GET" class="flex gap-2"><input name="search" value="{{ request('search') }}" placeholder="Kode, customer" class="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2 text-white"><select name="status" class="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2 text-white"><option value="">Semua status</option>@foreach(['pending','paid','completed','cancelled'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ $status }}</option>@endforeach</select><button class="rounded-lg bg-violet-600 px-3 py-2">Filter</button></form></div>
    <div class="mt-6 overflow-x-auto rounded-xl border border-slate-800"><table class="w-full text-left text-sm"><thead class="bg-slate-900 text-slate-400"><tr><th class="p-4">Kode</th><th class="p-4">Customer</th><th class="p-4">Item</th><th class="p-4">Total</th><th class="p-4">Order</th><th class="p-4">Payment</th><th class="p-4"></th></tr></thead><tbody class="divide-y divide-slate-800">@foreach($orders as $order)<tr><td class="p-4 font-semibold text-white">{{ $order->order_code }}</td><td class="p-4">{{ $order->user->email }}</td><td class="p-4">{{ $order->items_count }}</td><td class="p-4">Rp{{ number_format((float) $order->total_amount, 0, ',', '.') }}</td><td class="p-4">{{ $order->status }}</td><td class="p-4">{{ $order->payment?->status }}</td><td class="p-4"><a href="{{ route('admin.orders.show', $order) }}" class="text-cyan-300">Detail</a></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $orders->links() }}</div>
@endsection

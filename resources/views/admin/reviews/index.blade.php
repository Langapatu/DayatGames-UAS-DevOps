@extends('layouts.app')
@section('title', 'Reviews — Admin DayatGames')
@section('content')
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm uppercase tracking-widest text-amber-300">Moderasi</p><h1 class="text-3xl font-black text-white">Reviews</h1></div><form method="GET"><select name="status" class="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2 text-white"><option value="">Semua status</option>@foreach(['pending','published','rejected'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ $status }}</option>@endforeach</select><button class="ml-2 rounded-lg bg-violet-600 px-3 py-2">Filter</button></form></div>
    <div class="mt-6 space-y-4">@forelse($reviews as $review)<article class="rounded-xl border border-slate-800 bg-slate-900 p-5"><div class="flex flex-wrap justify-between gap-3"><div><h2 class="font-bold text-white">{{ $review->game->title }}</h2><p class="text-sm text-slate-400">{{ $review->user->name }} · {{ $review->rating }}/5</p></div><form method="POST" action="{{ route('admin.reviews.update', $review) }}" class="flex gap-2">@csrf @method('PUT')<select name="status" class="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2 text-white">@foreach(['pending','published','rejected'] as $status)<option value="{{ $status }}" @selected($review->status === $status)>{{ $status }}</option>@endforeach</select><button class="rounded-lg border border-slate-600 px-3 py-2">Simpan</button></form></div><p class="mt-4 text-slate-300">{{ $review->comment ?: 'Tanpa komentar.' }}</p></article>@empty<x-empty-state title="Tidak ada review" message="Belum ada review pada filter ini." />@endforelse</div><div class="mt-6">{{ $reviews->links() }}</div>
@endsection
